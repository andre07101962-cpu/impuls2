# 🧠 SERVER CAPABILITIES & CONSTRAINTS (v1.0)

Этот документ описывает **реальную** логику работы Бэкенда.
Используйте его для сверки с UI/UX Фронтенда.

---

## 1. ТИПЫ ПОСТОВ (POST TYPES)

Сервер поддерживает 5 типов контента (`PostType`). У каждого свои возможности и ограничения API Telegram.

### 🧟‍♂️ 1. STANDARD POST (`post`)
Самый гибкий тип.
*   **Контент:** Текст + Медиа (Фото/Видео).
*   **Режимы:**
    *   *Text Only:* Только текст.
    *   *Single Media:* Одно фото/видео + Подпись (Caption).
    *   *Album (Carousel):* От 2 до 10 медиа. Подпись прикрепляется к первому файлу.
*   **Кнопки (Inline Buttons):** ✅ Поддерживаются (URL кнопки).
    *   *Ограничение:* В режиме **Album** кнопки часто не отображаются или прикрепляются некорректно (Ограничение Telegram API). Рекомендуется блокировать кнопки для Альбомов на UI.
*   **Pin:** ✅ Можно закрепить.

### 👻 2. TELEGRAM STORY (`story`)
*   **Контент:** Строго **1 медиа** (Фото или Видео).
*   **Текст:** Подпись (Caption) до 200 символов (строгое ограничение TG).
*   **Настройки:** `story_config.period` (в секундах, обычно 86400).
*   **Кнопки:** ❌ НЕ ПОДДЕРЖИВАЮТСЯ.
*   **Pin:** ❌ НЕ ПОДДЕРЖИВАЕТСЯ.

### 🗳 3. POLL (`poll`)
*   **Контент:** Вопрос + Варианты ответов.
*   **Медиа:** ❌ НЕ ПОДДЕРЖИВАЕТСЯ (Опрос — это самостоятельный объект, нельзя прикрепить картинку).
*   **Текст:** ❌ НЕ ПОДДЕРЖИВАЕТСЯ (Нельзя добавить описание под опросом, только вопрос).
*   **Поля:**
    *   `question`: Строка (1-300 символов).
    *   `poll_options`: Массив строк (2-10 вариантов).
*   **Режимы:**
    *   `regular` (Обычный).
    *   `quiz` (Викторина) — требует `correct_option_id`.
*   **Настройки:** Анонимность, Множественный выбор.
*   **Pin:** ✅ Можно закрепить.

### 📄 4. DOCUMENT (`document`)
*   **Контент:** Файл (любой формат) отправляется "как документ" (без сжатия).
*   **Медиа:** Строго **1 файл** (URL).
*   **Текст:** Подпись (Caption).
*   **Кнопки:** ✅ Поддерживаются.
*   **Pin:** ✅ Можно закрепить.

### ⭐️ 5. PAID MEDIA (`paid_media`)
*   **Контент:** Фото/Видео, скрытые под "звездами".
*   **Медиа:** 1 или несколько файлов.
*   **Текст:** Подпись (Caption).
*   **Цена:** `paid_config.star_count` (Обязательно > 0).
*   **Pin:** ✅ Можно закрепить.

---

## 2. МАТРИЦА СОВМЕСТИМОСТИ (FEATURE MATRIX)

| Функция UI | Post (Text) | Post (Media) | Post (Album) | Story | Poll | Document | Paid Media |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **Текст/Caption** | ✅ | ✅ | ✅ (на 1-м) | ✅ (<200 ch) | ❌ (Только вопрос) | ✅ | ✅ |
| **Медиа (Файлы)** | ❌ | ✅ (1) | ✅ (2-10) | ✅ (1) | ❌ | ✅ (1) | ✅ |
| **Inline Buttons** | ✅ | ✅ | ⚠️ (Баги API) | ❌ | ❌ | ✅ | ✅ |
| **Pin (Закреп)** | ✅ | ✅ | ✅ | ❌ | ✅ | ✅ | ✅ |
| **Spoilers** | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ✅ |
| **Auto-Delete** | ✅ | ✅ | ✅ | ❌ (Свой таймер) | ✅ | ✅ | ✅ |

---

## 3. ЖИЗНЕННЫЙ ЦИКЛ (LIFECYCLE & EDITING)

Как сервер обрабатывает изменения после создания.

### 🕒 А. Режим "Scheduled" (Запланировано)
Пост лежит в БД, задача в Redis ждет своего часа.
*   **Что можно менять:** АБСОЛЮТНО ВСЁ.
*   **Логика:** Сервер просто перезаписывает JSON в базе и пересоздает таймеры в Redis.

### 🔴 Б. Режим "Published" (Live Edit)
Пост уже в Telegram канале.
*   **Эндпоинт:** `PATCH /publisher/schedule/:id` с флагом `isLiveEdit: true`.
*   **Текст:** ✅ **МОЖНО.** Сервер вызывает `editMessageText` или `editMessageCaption`.
*   **Кнопки:** ⚠️ **ЧАСТИЧНО.** Технически можно, но текущая реализация сервера обновляет только текст.
*   **Медиа:** ❌ **НЕЛЬЗЯ.** Telegram API не позволяет легко менять тип медиа (например, фото на видео) или состав альбома без пересоздания поста. Сервер сейчас это игнорирует.
*   **Опросы:** ❌ **НЕЛЬЗЯ.** Telegram запрещает менять варианты ответов или вопрос в опубликованном опросе. Только закрыть опрос.
*   **Таймер удаления:** ✅ **МОЖНО.** Можно добавить/изменить/удалить таймер смерти поста.
*   **Pin/Unpin:** ⚠️ Пока не реализовано в Live Edit (только при первой публикации).

### 🗑 В. Удаление
*   **Scheduled:** Отменяет публикацию, удаляет из базы.
*   **Published:** Пытается удалить сообщение из канала (`deleteMessage`). Если сообщение старше 48 часов или бот не админ — может не сработать.

---

## 4. СТРУКТУРА ДАННЫХ (JSON CONTRACT)

Пример идеального JSON, который ждет Бэкенд.

```json
{
  "type": "poll", // post | story | poll | document | paid_media
  "channelIds": ["-100123456789"],
  "publishAt": "2024-10-25T15:00:00.000Z",
  "deleteAt": "2024-10-25T18:00:00.000Z", // Опционально
  
  "content": {
    // --- ДЛЯ ПОСТОВ / ДОКУМЕНТОВ ---
    "text": "Hello World", 
    "media": ["https://url.com/img.jpg"], // Строка или Массив

    // --- ДЛЯ ОПРОСОВ ---
    "question": "React vs Vue?",
    "poll_options": ["React", "Vue"], // ⚠️ НЕ "options"!
    "poll_config": {
      "is_anonymous": true,
      "type": "quiz",
      "correct_option_id": 0
    },

    // --- ДЛЯ СТОРИС ---
    "story_config": { "period": 86400 },

    // --- ДЛЯ PAID MEDIA ---
    "paid_config": { "star_count": 50 },

    // --- ОБЩИЕ НАСТРОЙКИ ---
    "options": {
      "pin": true,                // Закрепить после публикации
      "disable_notification": true, // Silent
      "protect_content": true     // Запрет пересылки
    }
  }
}
```