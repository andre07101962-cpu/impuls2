
# 📚 Полная Спецификация API и Жизненных Циклов (Impulse)

Этот документ описывает все возможности бэкенда, необходимые для реализации на клиенте.

---

## 🔐 МОДУЛЬ 1: АВТОРИЗАЦИЯ И БОТЫ

### 1.1. Вход в систему (Login Flow)
**Жизненный цикл:**
1.  Пользователь открывает бота в Telegram и нажимает `/start`.
2.  Бот присылает сообщение: "Login ID: `12345`, Token: `abc...`".
3.  Пользователь вводит эти данные в форму входа на сайте.
4.  **Frontend:** Отправляет `POST /auth/login`.
5.  **Backend:** Проверяет хэш токена. Возвращает объект пользователя.
6.  **Frontend:** Сохраняет токен (Bearer).

### 1.2. Подключение Бота (Add Bot)
Пользователь подключает своего бота для управления каналами.
**Жизненный цикл:**
1.  Пользователь создает бота в @BotFather и получает токен (например, `123:ABC...`).
2.  **Frontend:** Форма ввода токена -> `POST /bots`.
3.  **Backend:**
    *   Проверяет валидность токена в Telegram.
    *   Устанавливает Webhook (чтобы слышать события).
    *   Сохраняет бота в БД.
4.  **Frontend:** Получает объект бота и добавляет его в список.

### 1.3. Список Ботов
*   **API:** `GET /bots`
*   **Использование:** Для переключателя "От имени какого бота мы сейчас работаем".

---

## 📢 МОДУЛЬ 2: КАНАЛЫ (CHANNELS)

### 2.1. Добавление Канала (Discovery)
**Сценарий:** Пользователь хочет добавить канал в дашборд.
**Жизненный цикл:**
1.  Пользователь добавляет Бота в администраторы канала в Telegram.
2.  **Backend (Webhook):** Получает событие `my_chat_member` и **автоматически** создает канал в БД.
3.  **Frontend:** Пользователь нажимает кнопку "Обновить/Sync" (`POST /channels/sync`).
4.  Список каналов обновляется (`GET /channels`).

*Альтернатива (Ручная проверка):*
1.  **Frontend:** Вводит @username канала в модалке -> `POST /channels/preview`.
2.  Отображается превью (Название, фото).
3.  Кнопка "Добавить" -> `POST /channels/add`.

### 2.2. Профиль Канала (Настройки)
**Возможности:** Изменение названия и описания.
*   **API:** `PATCH /channels/profile`
*   **Payload:** `{ botId, channelId, title, description }`
*   **Примечание:** Аватар меняется через Telegram API (на бэкенде пока нет загрузки файлов, используем ссылку, если нужно).

### 2.3. Глобальные права (Permissions)
**Для чего:** Настройка того, что могут делать обычные участники в Группе.
*   **API:** `POST /channels/permissions`
*   **Payload:** `{ permissions: { can_send_messages: boolean, can_send_media: boolean, ... } }`
*   **UI:** Список переключателей (Toggles).

### 2.4. Модерация (Люди)
Управление пользователями чата.

1.  **Список Админов:** `GET /channels/:id/admins`. (Грузится в реальном времени).
2.  **Забанить (Ban):**
    *   **API:** `POST /channels/ban`
    *   **Параметры:** `targetUserId`, `untilDate` (дата разбана, если пусто — навсегда).
3.  **Разбанить (Unban):** `POST /channels/unban`.
4.  **Повысить до Админа (Promote):**
    *   **API:** `POST /channels/promote`
    *   **Параметры:** `customTitle` (должность), `permissions` (права админа).
5.  **Ограничить (Mute/Restrict):**
    *   **API:** `POST /channels/restrict` (аналогично бану, но с выбором прав `can_send_messages: false`).
6.  **Выгнать Бота (Leave):** `POST /channels/leave`. (Опасная зона).

### 2.5. Ссылки-приглашения (Invite Links)
Маркетинговые ссылки для отслеживания входов.
*   **Создать:** `POST /channels/invite-link`
    *   Параметры: `name` (название для себя), `expireDate`, `limit` (кол-во вступлений).

---

## 💬 МОДУЛЬ 3: ФОРУМЫ И ТОПИКИ (TOPICS)

Работает только если у канала `isForum: true`.

### 3.1. CRUD Топиков
1.  **Список:** `GET /channels/:id/topics`.
2.  **Создать:** `POST /channels/topic`
    *   Input: Название, Цвет иконки (RGB int), Custom Emoji ID.
3.  **Редактировать:** `PATCH /channels/topic/:id` (Имя, иконка).
4.  **Удалить:** `DELETE /channels/topic/:id` (Удаляет ветку целиком!).

### 3.2. Управление состоянием
1.  **Закрыть (Close):** `POST .../close`. Ветка становится read-only для пользователей.
2.  **Открыть (Reopen):** `POST .../reopen`.

---

## ✍️ МОДУЛЬ 4: ПУБЛИКАЦИЯ (PUBLISHER)

Самый сложный модуль. Единая точка входа для создания контента.

### 4.1. Создание Поста (Schedule)
**API:** `POST /publisher/schedule`

**Жизненный цикл:**
1.  Пользователь заполняет форму (выбирает тип, каналы, время).
2.  **Frontend** формирует JSON `contentPayload` в зависимости от типа.
3.  **Backend:** Создает задачу в очереди (Status: `SCHEDULED`).
4.  ...Время ожидания...
5.  **Backend (Processor):** Публикует пост в Telegram.
6.  Статус меняется на `PUBLISHED`. Появляется `tgMessageId`.

### 4.2. Типы Постов и их Поля (Payload)

В поле `content` нужно передавать разные данные в зависимости от `type`:

| Тип (`type`) | Обязательные поля в `content` | Описание |
| :--- | :--- | :--- |
| **POST** | `text` (HTML), `media` (Array of URLs) | Обычный пост или Альбом (если >1 фото). |
| **STORY** | `media` (1 URL), `text` | Telegram Story (видео/фото 9:16). |
| **POLL** | `question`, `poll_options` (Array), `poll_config` | Опрос или Викторина. |
| **PAID_MEDIA**| `media`, `text`, `paid_config.star_count` | Платный пост за "Звезды". |
| **AUDIO** | `media` (URL), `performer`, `title`, `thumbnail` | Музыкальный файл. |
| **VOICE** | `media` (URL .ogg), `duration` | Голосовое сообщение. |
| **VIDEO_NOTE**| `media` (URL .mp4 квадрат), `length` | "Кружочек". |
| **LOCATION** | `latitude`, `longitude` | Геоточка. |
| **CONTACT** | `phone_number`, `first_name` | Визитка контакта. |
| **COPY** | `from_chat_id`, `message_id` | Скопировать пост из другого канала (Steal). |
| **FORWARD** | `from_chat_id`, `message_id` | Переслать пост (Repost). |

**Общие настройки (`content.options`):**
*   `pin`: true/false (Закрепить после отправки).
*   `disable_notification`: true/false (Тихий режим).
*   `protect_content`: true/false (Запрет пересылки/копирования).
*   `topic_id`: ID топика (если публикуем в форум).

### 4.3. Редактирование (Edit)
Есть два режима, которые должен различать фронтенд.

**Сценарий А: Пост еще не вышел (Status: SCHEDULED)**
*   **Действие:** Полное редактирование.
*   **API:** `PATCH /publisher/schedule/:id`
*   **Можно менять:** ВСЁ (Время, Каналы, Тип, Медиа).

**Сценарий Б: Пост уже опубликован (Status: PUBLISHED)**
*   **Действие:** "Live Edit" (исправление ошибок).
*   **API:** `PATCH /publisher/schedule/:id`
*   **Payload:** `{ isLiveEdit: true, content: { text: "New Text" } }`
*   **Можно менять:** Только Текст, Кнопки (Inline Keyboard).
*   **Нельзя менять:** Тип поста (Фото -> Видео), Каналы.

### 4.4. Удаление (Delete)
*   **API:** `DELETE /publisher/:id`
*   **Если Scheduled:** Просто отменяет таймер.
*   **Если Published:** Удаляет сообщение из Telegram (для всех).

---

## 📊 МОДУЛЬ 5: КАЛЕНДАРЬ (VIEW)

**API:** `GET /publisher/publications`

Фронтенд должен отображать посты в календаре, крася их в зависимости от `status`:
*   🔵 **Scheduled:** Будущие посты.
*   🟢 **Published:** Уже в канале.
*   🔴 **Failed:** Ошибка (например, бот кикнут).
*   ⚪ **Deleted:** Удален.
