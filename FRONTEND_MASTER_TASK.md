# 🚀 IMPULSE FRONTEND: Master Specification
**Target:** Ultimate Telegram Management Dashboard.
**Role:** Frontend Developer
**Architecture:** Reactive UI (Master-Slave Sync)

---

## 🎯 Глобальная цель
Реализовать интерфейс, полностью заменяющий настройки канала внутри Telegram.
**Принцип:** "Если это есть в Telegram API, это должно быть у нас".

---

## 📦 Модуль 1: Authentication
*(Без изменений, см. предыдущую версию)*
Login via Telegram ID + Token flow.

---

## 📢 Модуль 2: Channel Dashboard (Главный Экран)
Вместо простой карточки, при клике на канал мы проваливаемся в **Full Dashboard**.

### Структура Детальной Страницы Канала:

#### 🟢 Tab 1: Overview (Сводка)
*   **Header:** Аватар, Название, Username, Кол-во подписчиков.
*   **Actions:**
    *   Кнопка "Sync" (Force Refresh).
    *   Кнопка "Open in TG" (ссылка).
*   **Stats Widget:** График прироста (mock data пока, но заложить компонент).
*   **Recent Activity:** Лог последних действий бота (кто забанен, что запостили).

#### 🟡 Tab 2: Content (Постинг)
*   Интеграция с **Модулем 3** (Publisher).
*   Calendar View / List View постов.
*   Фильтры по статусам (Scheduled, Published).

#### 🔵 Tab 3: Forum (Только если `isForum: true`)
*   Таблица топиков.
*   Колонки: Icon, Name, Messages Count (mock), Status (Open/Closed), Actions.
*   **Context Menu:** Rename, Change Icon, Close/Reopen, Delete, Unpin All Messages.
*   **Special:** Кнопка редактирования "General" топика.

#### 🟣 Tab 4: Settings (Настройки)
*   **General:**
    *   Input: Title.
    *   Textarea: Description.
    *   Upload: Photo (Dropzone). Кнопка "Remove Photo".
    *   Button: "Save Changes".
*   **Permissions (Группы):**
    *   Секция "Default Permissions" с тоглами (Send Messages, Media, Polls, etc.).
*   **Bot Menu:**
    *   Настройка кнопки меню бота для этого чата.

#### 🟠 Tab 5: People (Люди)
*   **Sub-tab: Admins:**
    *   Список админов.
    *   Кнопка "Promote Admin" (Поиск юзера по ID -> Выбор прав -> Save).
    *   Редактирование "Custom Title" (подпись админа).
*   **Sub-tab: Banned:**
    *   Список забаненных ("Черный список").
    *   Кнопка "Unban".
    *   Кнопка "Ban User" (Input ID + Duration).
*   **Sub-tab: Join Requests:**
    *   Список заявок.
    *   Кнопки "Approve" / "Decline" для каждой.
    *   Кнопка "Approve All".

#### 🟤 Tab 6: Growth (Рост)
*   **Invite Links Manager:**
    *   Таблица ссылок: Name, Link (copy btn), Views/Joins (если API отдает), Active/Revoked.
    *   Create Link Modal:
        *   Input Name.
        *   Datepicker (Expire).
        *   Input Number (Usage Limit).
        *   Toggle "Request Admin Approval".

---

## ✍️ Модуль 3: Creator Studio (Publisher)
*(Логика постинга остается из FRONTEND_PUBLISHER.md, но интегрируется в Tab 2)*

**Дополнение:**
*   Добавить возможность **Pin Message** в уже опубликованном посте (отдельная кнопка в списке постов).
*   Добавить возможность **Delete Message** (удалить из TG).

---

## 🎨 Design System & UX
*   **Modals:** Используем модальные окна для всех "опасных" или сложных действий (Promote Admin, Ban User, Edit Settings).
*   **Toasts:** На каждое успешное действие (Link Copied, Settings Saved) и ошибку.
*   **Loading States:** Поскольку некоторые запросы в TG могут идти 1-2 секунды, кнопки должны иметь состояние `isLoading` (спиннер).

---

## 🛠 Техническое задание (Steps)
1.  Создать Layout с Sidebar (список каналов) и Main Area (дашборд выбранного канала).
2.  Реализовать роутинг: `/channel/:id/overview`, `/channel/:id/settings` и т.д.
3.  Реализовать формы Settings (Photo Upload - `multipart/form-data`).
4.  Реализовать таблицы для Admins/Invites/Topics.
5.  Реализовать логику Permission Toggles (битовая маска или JSON объект прав).

